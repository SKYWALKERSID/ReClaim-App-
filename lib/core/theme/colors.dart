import 'package:flutter/material.dart';

class AppColors {
  // Vibrant Neon Palette
  static const primary = Color(0xFF00D2FF); // Electric Blue
  static const primaryDark = Color(0xFF3A7BD5);
  static const primaryLight = Color(0xFFE7F0FF); // Kept for compatibility
  static const accent = Color(0xFF00FFD1); // Neon Teal
  static const glow = Color(0x4000D2FF);
  
  // Backgrounds
  static const background = Color(0xFF020617); // Ultra Dark Navy
  static const darkSurface = Color(0xFF0F172A); // Slate Navy
  static const surface = Color(0xFFFFFFFF); // Kept for compatibility
  static const glass = Color(0x1AFFFFFF); // Glassmorphism white
  static const glassDark = Color(0x33000000); // Glassmorphism black

  // Text
  static const textPrimary = Color(0xFFF8FAFC);
  static const textSecondary = Color(0xFF94A3B8);
  
  // States
  static const warning = Color(0xFFFF5E7D); // Neon Pink/Red
  static const success = Color(0xFF10B981);
}