/** Synthetic principal when `x-api-key` is used (native Android / machine clients). */
export const API_KEY_SERVICE_USER = {
    userId: "00000000-0000-4000-8000-000000000001",
    role: "service",
};
